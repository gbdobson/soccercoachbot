version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No frontend build step. Serving static index.html."
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
